replicaCount: 3

image:
  tag: git-locked   # controlled promotion later

resources:
  requests:
    cpu: 300m
    memory: 512Mi
  limits:
    cpu: 500m
    memory: 1Gi

ingress:
  enabled: true
  host: gfj.example.com
  tls:
    enabled: true
    secretName: gfj-prod-tls